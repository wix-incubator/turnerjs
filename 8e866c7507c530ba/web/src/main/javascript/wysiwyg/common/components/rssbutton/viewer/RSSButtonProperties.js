define.dataSchema('RssButtonProperties', function() {
    return {
        "transition": {
            "enum": ["none", "fade"],
            "default": "fade",
            "type": "string"
        }
    };
});

