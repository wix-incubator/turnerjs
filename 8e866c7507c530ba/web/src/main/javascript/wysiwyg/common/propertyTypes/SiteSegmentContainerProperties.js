define.dataSchema('SiteSegmentContainerProperties', {
    fixedPosition: {
        'type': 'boolean',
        'default': false,
        'description': 'fixed positioning for components that inherit from SiteSegmentContainers'
    }
});

