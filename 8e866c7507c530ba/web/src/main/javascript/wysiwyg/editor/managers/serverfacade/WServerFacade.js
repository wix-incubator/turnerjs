/**
 *  this is just a temporary file until the new version is deployed to the server
 */

define.Class('wysiwyg.editor.managers.serverfacade.WServerFacade', function(classDefinition){
    /** @type bootstrap.managers.classmanager.ClassDefinition */
    var def = classDefinition;

    def.inherits('wysiwyg.editor.managers.serverfacade.ServerFacade');

});
