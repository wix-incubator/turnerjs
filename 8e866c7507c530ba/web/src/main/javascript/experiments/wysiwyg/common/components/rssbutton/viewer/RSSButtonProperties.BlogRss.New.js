define.experiment.newDataSchema('RssButtonProperties.BlogRss.New', function() {
    return {
        "transition": {
            "enum": ["none", "fade"],
            "default": "fade",
            "type": "string"
        }
    };
});

