define.experiment.Class('wysiwyg.editor.managers.WPreviewManager.SocialActivity', function(classDefinition, experimentStrategy){
    var def = classDefinition;
    var strategy = experimentStrategy;

//    def.utilize(strategy.merge(['wysiwyg.managers.SocialActivityManager']));
//
//    def.methods({
//        _init: strategy.after(function(){
//            if(!W.SocialActivityManager){
//                W.SocialActivityManager = new this.imports.SocialActivityManager({param: 'test'});
//            }
//        })
//    });
});
