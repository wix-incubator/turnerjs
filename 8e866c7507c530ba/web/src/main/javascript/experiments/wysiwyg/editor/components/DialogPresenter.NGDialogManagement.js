define.experiment.component('wysiwyg.editor.components.DialogPresenter.NGDialogManagement', function (componentDefinition) {
    /**@type core.managers.component.ComponentDefinition*/
    var def = componentDefinition;

    def.methods({
        _setDialogViewToManager: function () {
            // dialog layer set moved to EditorPresenter as part of the angular dialogs framework
        }
    });
});