define.experiment.component('wysiwyg.viewer.components.HorizontalMenu.DisableHorizontalMenu', function (componentDefinition) {

    var def = componentDefinition;

    def.statics({
        EDITOR_META_DATA: {
            general: {
                settings: false,
                design: false,
                animation: false
            }
        }
    });

});